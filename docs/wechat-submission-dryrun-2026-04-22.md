# 微信小程序提审 Dry Run — Blocker 清单

**日期**: 2026-04-22
**基线**: 微信小程序 Jest 181/181 全绿 ✅
**生成方式**: 工程 agent 全量盘点（`app.json` / `project.config.json` / 法律页面 / 提审材料目录 / 路由可达性 + 微信审核 checklist 逐条核对）

---

## 一、已完成项 ✅

| # | 项目 | 状态 / 证据 |
|---|------|------|
| A1 | 设置页 → 隐私政策入口 | ✅ commit `5a73817` 周边 |
| A2 | 设置页 → 用户协议入口 | ✅ commit `5a73817` |
| A3 | 关于页 → 用户协议 / 隐私政策入口 | ✅ commit `972b03d` |
| B4 | 法律页面动态日期 + 版本号 v1.0 | ✅ commit `3bdbb6f` |
| — | `app.json` → `__usePrivacyCheck__: true` | ✅ |
| — | 账号注销页面（OTP 验证 + 长按确认 + 30 天恢复期） | ✅ `pages/settings/delete-account` |
| — | 手机号绑定流程 + 多入口校验（下单 / 接单 / 抢单） | ✅ commit `e919539` (D-034) |
| — | 登录页协议勾选链接 | ✅ |
| — | 隐私 / 用户协议完整页面 | ✅ |
| — | 高风险文案扫描（§8 禁止词 / 高风险词） | ✅ 零命中 |
| — | `packOptions` 排除 `__tests__` / `node_modules` | ✅ |
| — | Jest 测试 181 / 181 全绿 | ✅ |

---

## 二、剩余 Blocker 清单

### 🔴 P0 — 提审硬性阻塞（不解决 = 无法上传 / 必拒）

| # | Blocker | 说明 | Owner |
|---|---------|------|-------|
| **P0-1** | **AppID 占位符** | `project.config.json` 中 `appid = wx0000000000000000`，必须替换为正式申请的 AppID | 运维 / PM |
| **P0-2** | **域名 ICP 备案 + 微信白名单** (B-04) | `api.yiluan.com` 未备案；微信后台 request / socket / upload / download 域名全部待配。**ICP 备案 10–20 天**，是关键路径上最长的一段 | 运维 |
| **P0-3** | **服务类目 + 资质材料** | 营业执照、医院合作协议、陪诊师执业证明等全部未上传，类目（医疗 - 互联网医院 / 健康服务）未选 | 法务 / 运营 |
| **P0-4** | **微信支付商户号** (B-01) | 商户号未申请，支付回调 URL 未配，当前后端仅 mock 支付通过 | 运维 / 财务 |
| **P0-5** | **隐私弹窗真机联调未验证** | `__usePrivacyCheck__` 已开，但公众平台需先**发布"用户隐私保护指引"**才会触发弹窗；当前真机未联调 | PM / 前端 |

### 🟡 P1 — 强烈建议（直接影响过审率）

| # | Issue | 说明 | Owner |
|---|-------|------|-------|
| **P1-1** | 关于页缺 ICP 备案号 + 公司主体名称 | checklist §10.3 要求；待 P0-2 备案完成后填入 | 前端 |
| **P1-2** | 隐私协议字段与 `chooseImage` 实际调用 (3 处头像上传) 比对 | §4.1 表中相机 / 相册权限项标注 ⚠️ 待确认 | PM |
| **P1-3** | `wx.requirePrivacyAuthorize` 未显式调用 | 全局搜索零匹配；2023 新隐私规范部分场景需主动调用，需确认框架是否已自动处理 | 前端 |
| **P1-4** | "联系客服"入口无实际实现 | 法律文本多处写"我的 → 联系客服"，但实际无 `open-type="contact"` 按钮或客服页面；电话 `400-000-0000` 是占位 | PM / 前端 |
| **P1-5** | 提审截图 0 / 6 | `wechat/submission/screenshots/` 下 12 项全 TODO | PM / 设计 |
| **P1-6** | SMS 真实通道 (B-02) | 阿里云 SMS AccessKey 未配，审核员实测发送验证码会失败（除非走开发期 `000000` 万能码） | 运维 |

### 🟢 P2 — 锦上添花

| # | Item |
|---|------|
| P2-1 | 法律协议 H5 镜像（方便外部分享） |
| P2-2 | `sitemap.json` 当前 allow all，可考虑收紧到核心页 |

---

## 三、关键路径与日历估算

```
ICP 备案 (10-20 天) ────────────────────────────────┐
微信支付商户号申请 (3-5 天) ─────┐                    │
资质材料收集 (法务, ~5 天) ──┐    │                    │
                            ├─ 微信后台配置 ─┬─ 上传代码 → 提审
隐私弹窗联调 (~1 天) ────────┘    │            │
截图准备 (~0.5 天) ───────────────┘            │
AppID 替换 (~5 min) ──────────────────────────┘
```

**最长杆**：**ICP 备案 (B-04) 10–20 天**。其余 P0 / P1 项均可并行执行。

---

## 四、模拟提审通过率评估

- **当前通过率：约 15%**
  - 工程侧（页面、隐私接入、协议入口、注销）已 ≥ 90% 就绪
  - 但 P0-1 / P0-2 / P0-3 / P0-4 任一未完成都会导致 **0 通过**（无法上传或必拒）
  - 即使绕过 0 通过，缺截图、缺客服、缺备案号也会大概率被退回补材料
- **若 P0 全清 + P1-1~P1-5 完成，预计通过率：约 75–85%**
- **建议**：等 ICP / 类目 / 商户号 三大资质同步前进，工程侧补 P1-3~P1-5；启动二轮联调时再做 dry run

---

## 五、推荐下一步动作（按优先级）

1. **文龙现在能立刻动的**：把 P0-1（AppID 替换）、P0-3（启动法务出材料）、P0-4（微信支付商户号申请）、P0-2（ICP 备案启动）四个流程**今日全部踢出去**——因为它们都不是工程能解决，启动越早越好
2. **工程侧本周可推进**：P1-3（确认 `wx.requirePrivacyAuthorize` 是否需补）、P1-4（实现客服入口 `open-type="contact"` + 客服页面）、P1-5（让设计出 6 张提审截图）
3. **运维侧可并行**：B-02 阿里云 SMS AccessKey 申请（不影响审核但影响验收）

---

## 六、外部依赖项（非工程能解决，需文龙出面）

| 项 | 责任方 | 预估工期 | 当前状态 |
|---|---|---|---|
| 微信支付商户号 (B-01) | 运维 / 财务 | 3-5 天 | 未启动 |
| 阿里云 SMS AccessKey (B-02) | 运维 | 1-3 天 | 未启动 |
| 苹果开发者账号 (B-05) | 运维 / 财务 | 3-7 天（含 D-U-N-S） | 未启动 |
| 域名 ICP 备案 (B-04) | 运维 | 10-20 天 | 未启动 |
| 医疗类目资质 + 医院合作协议 (P0-3) | 法务 / 运营 | ~5 天 | 未启动 |

---

**报告生成**：Claude Code agent (`delta-dune`) 全量盘点 + 主控核对落盘
**关联文件**：`Q:\OpenClaw\task-data\daily-outputs\engineering-health\2026-04-22.md`（同日工程体检报告）
