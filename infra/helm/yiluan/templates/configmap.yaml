apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "yiluan.fullname" . }}-config
  labels:
    {{- include "yiluan.labels" . | nindent 4 }}
data:
  app.yaml: |
{{ toYaml .Values.config | indent 4 }}
