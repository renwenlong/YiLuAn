# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Validation & Settings
pydantic==2.10.4
pydantic-settings==2.7.1

# Auth
PyJWT==2.9.0
passlib[bcrypt]==1.7.4

# Cryptography (explicit — used for WeChat Pay signature verification & AES-GCM decryption)
cryptography>=43.0.0

# Redis
redis[hiredis]==5.2.1

# Azure
azure-storage-blob==12.24.0

# HTTP client (for APNs etc)
httpx==0.28.1

# SMS SDKs
alibabacloud-dysmsapi20170525>=3.0.0
tencentcloud-sdk-python-sms>=3.0.0

# Monitoring
prometheus_client>=0.20.0

# Rate limiting
slowapi==0.1.9

# YAML for config
pyyaml==6.0.2

# Background task scheduler (D-018)
APScheduler>=3.10,<4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0

# Dev
black==24.10.0
ruff==0.8.6
