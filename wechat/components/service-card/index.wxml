<view class="service-card {{active ? 'active' : ''}}" wx:if="{{label}}">
  <view class="service-info">
    <text class="service-label">{{label}}</text>
    <text class="service-desc">{{desc}}</text>
  </view>
  <text class="service-price">{{priceText}}</text>
</view>
