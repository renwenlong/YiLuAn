<view class="service-card {{active ? 'active' : ''}}" wx:if="{{info.label}}">
  <view class="service-info">
    <text class="service-label">{{info.label}}</text>
    <text class="service-desc">{{info.desc}}</text>
  </view>
  <text class="service-price">¥{{info.price}}</text>
</view>
