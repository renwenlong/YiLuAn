<view wx:if="{{visible}}" class="network-banner" bindtap="onTapRetry">
  <text class="network-banner__icon">⚠️</text>
  <text class="network-banner__text">{{t['networkBanner.unstable']}}</text>
  <text class="network-banner__action">{{t['networkBanner.retry']}}</text>
</view>
