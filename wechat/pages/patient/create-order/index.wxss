/* ============================================
 * Create Order — Upgraded UI v2.0
 * ============================================ */

.page-container {
  min-height: 100vh;
  background-color: var(--color-bg-page);
  padding: var(--spacing-md);
  padding-bottom: 180rpx;
  box-sizing: border-box;
}

/* ---- Info Section ---- */
.info-section {
  margin-bottom: var(--spacing-lg);
}

.section-label {
  display: block;
  font-size: var(--font-size-body);
  color: var(--color-text-secondary);
  margin-bottom: var(--spacing-sm);
  font-weight: 500;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-sm);
}

.section-header .section-label {
  margin-bottom: 0;
}

.change-btn {
  font-size: var(--font-size-caption);
  color: var(--color-text-link);
  transition: opacity var(--duration-fast) var(--ease-in-out);
}

.change-btn:active {
  opacity: 0.7;
}

/* ---- Info Card ---- */
.info-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg) var(--spacing-md);
  box-shadow: var(--shadow-sm);
  border-left: 4rpx solid var(--color-primary);
  transition: transform var(--duration-fast) var(--ease-in-out),
              box-shadow var(--duration-fast) var(--ease-in-out);
}

.info-card:active {
  transform: scale(0.98);
  box-shadow: var(--shadow-xs);
}

.info-card.placeholder {
  opacity: 0.6;
  border-left-color: var(--color-border);
}

.info-name {
  font-size: var(--font-size-h2);
  font-weight: 600;
  color: var(--color-text-primary);
}

.placeholder-text {
  color: var(--color-text-hint);
  font-weight: 400;
}

.info-price {
  font-size: var(--font-size-h2);
  font-weight: 700;
  color: var(--color-accent);
  background: rgba(255, 122, 69, 0.08);
  padding: var(--spacing-xs) var(--spacing-sm);
  border-radius: var(--radius-full);
}

/* ---- Companion Info Card ---- */
.companion-info-card {
  display: flex;
  align-items: center;
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-md);
  box-shadow: var(--shadow-sm);
  border-left: 4rpx solid var(--color-primary);
  transition: transform var(--duration-fast) var(--ease-in-out);
}

.companion-info-card:active {
  transform: scale(0.98);
}

.avatar-placeholder {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: var(--gradient-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: var(--spacing-md);
  flex-shrink: 0;
  box-shadow: var(--shadow-primary);
}

.avatar-placeholder.small {
  width: 64rpx;
  height: 64rpx;
  box-shadow: none;
}

.avatar-text {
  font-size: var(--font-size-h2);
  color: var(--color-text-white);
  font-weight: 600;
}

.avatar-placeholder.small .avatar-text {
  font-size: var(--font-size-caption);
}

.companion-detail {
  flex: 1;
  min-width: 0;
}

.companion-name {
  display: block;
  font-size: var(--font-size-h2);
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: var(--spacing-xs);
}

.companion-meta {
  display: flex;
  gap: var(--spacing-md);
}

.companion-meta .meta-item {
  font-size: var(--font-size-caption);
  color: var(--color-text-hint);
}

/* ---- Picker ---- */
.picker-value {
  height: 88rpx;
  line-height: 88rpx;
  padding: 0 var(--spacing-md);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: var(--font-size-body);
  color: var(--color-text-primary);
  background: var(--color-bg-input);
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: border-color var(--duration-fast) var(--ease-in-out),
              box-shadow var(--duration-fast) var(--ease-in-out);
}

.picker-value:active {
  border-color: var(--color-border-focus);
  box-shadow: 0 0 0 4rpx rgba(24, 144, 255, 0.1);
}

.picker-arrow {
  font-size: var(--font-size-caption);
  color: var(--color-text-hint);
  transition: transform var(--duration-fast) var(--ease-in-out);
}

/* ---- Textarea ---- */
.form-textarea {
  width: 100%;
  min-height: 200rpx;
  padding: var(--spacing-md);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: var(--font-size-body);
  box-sizing: border-box;
  background: var(--color-bg-input);
  color: var(--color-text-primary);
  line-height: var(--line-height-relaxed);
  transition: border-color var(--duration-fast) var(--ease-in-out),
              box-shadow var(--duration-fast) var(--ease-in-out);
}

.form-textarea-focus {
  border-color: var(--color-border-focus);
  box-shadow: 0 0 0 4rpx rgba(24, 144, 255, 0.1);
  background: var(--color-bg-card);
}

.char-count {
  text-align: right;
  font-size: var(--font-size-small);
  color: var(--color-text-disabled);
  margin-top: var(--spacing-xs);
}

.char-count.near-limit {
  color: var(--color-warning);
}

.char-count.at-limit {
  color: var(--color-danger);
}

/* ---- Submit Area ---- */
.submit-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: var(--spacing-md) var(--spacing-lg);
  padding-bottom: calc(var(--spacing-md) + env(safe-area-inset-bottom));
  background: var(--color-bg-card);
  box-shadow: 0 -2rpx 16rpx rgba(0, 0, 0, 0.06);
  z-index: var(--z-fixed);
}

.submit-btn {
  width: 100%;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-h2);
  background: var(--gradient-primary);
  color: var(--color-text-white);
  border-radius: var(--radius-full);
  font-weight: 600;
  padding: 0;
  border: none;
  box-shadow: var(--shadow-primary);
  transition: opacity var(--duration-fast) var(--ease-in-out),
              transform var(--duration-fast) var(--ease-in-out);
}

.submit-btn::after {
  border: none;
}

.submit-btn:active {
  opacity: 0.85;
  transform: scale(0.98);
}

.submit-btn[disabled] {
  opacity: 0.4;
  box-shadow: none;
}

/* ---- Modal ---- */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--color-bg-mask);
  z-index: var(--z-modal);
  display: flex;
  align-items: flex-end;
}

.modal-content {
  width: 100%;
  max-height: 70vh;
  background: var(--color-bg-card);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  display: flex;
  flex-direction: column;
  animation: slideUp var(--duration-normal) var(--ease-spring);
}

.modal-handle {
  width: 64rpx;
  height: 8rpx;
  background: var(--color-border);
  border-radius: var(--radius-full);
  margin: var(--spacing-sm) auto 0;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-lg);
  border-bottom: 1rpx solid var(--color-border-light);
}

.modal-title {
  font-size: var(--font-size-h2);
  font-weight: 600;
  color: var(--color-text-primary);
}

.modal-close {
  font-size: var(--font-size-h1);
  color: var(--color-text-hint);
  padding: 0 var(--spacing-xs);
  transition: color var(--duration-fast) var(--ease-in-out);
}

.modal-close:active {
  color: var(--color-text-secondary);
}

.modal-body {
  padding: var(--spacing-sm) var(--spacing-lg) var(--spacing-xl);
  overflow-y: auto;
  max-height: 60vh;
}

/* ---- Companion Option ---- */
.companion-option {
  display: flex;
  align-items: center;
  padding: var(--spacing-md) var(--spacing-sm);
  border-bottom: 1rpx solid var(--color-border-light);
  border-radius: var(--radius-md);
  transition: background var(--duration-fast) var(--ease-in-out);
}

.companion-option:active {
  background: var(--color-bg-hover);
}

.companion-option.active {
  background: var(--color-primary-light);
}

.companion-option-info {
  flex: 1;
  min-width: 0;
}

.companion-option-header {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-xs);
}

.companion-option-name {
  font-size: var(--font-size-h2);
  font-weight: 600;
  color: var(--color-text-primary);
}

.verified-badge {
  font-size: var(--font-size-small);
  color: var(--color-primary);
  background: var(--color-primary-light);
  padding: var(--spacing-2xs) var(--spacing-sm);
  border-radius: var(--radius-full);
  font-weight: 500;
}

.companion-option-stats {
  display: flex;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-xs);
}

.companion-option-stats .stat-item {
  font-size: var(--font-size-caption);
  color: var(--color-text-hint);
}

.companion-option-bio {
  display: block;
  font-size: var(--font-size-caption);
  color: var(--color-text-secondary);
  margin-bottom: var(--spacing-xs);
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  line-height: var(--line-height-relaxed);
}

.companion-option-tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-xs);
}

.area-tag {
  font-size: var(--font-size-small);
  color: var(--color-primary);
  background: var(--color-primary-light);
  padding: var(--spacing-2xs) var(--spacing-sm);
  border-radius: var(--radius-sm);
}

.companion-option-check {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  border: 2rpx solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-left: var(--spacing-sm);
  flex-shrink: 0;
  transition: all var(--duration-fast) var(--ease-in-out);
}

.companion-option.active .companion-option-check {
  background: var(--gradient-primary);
  border-color: var(--color-primary);
  color: var(--color-text-white);
  font-size: var(--font-size-caption);
}

/* ---- Hints ---- */
.loading-hint {
  text-align: center;
  padding: var(--spacing-lg) 0;
  color: var(--color-primary);
  font-size: var(--font-size-body);
  animation: pulse 1.5s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.empty-hint {
  text-align: center;
  padding: var(--spacing-2xl) 0;
  color: var(--color-text-hint);
  font-size: var(--font-size-body);
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

/* ============================================
 * S2-INT-004 prelude · U-1 折叠 stepper
 * ============================================ */

.page-container.huge {
  font-size: var(--body-font, 14px);
}

.huge-font-toggle {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 12rpx;
  padding: 8rpx 16rpx;
  margin-bottom: var(--spacing-md);
}

.huge-font-label {
  font-size: var(--meta-font, 12px);
  color: var(--color-text-secondary);
}

.step-section {
  background: #fff;
  border-radius: 16rpx;
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-md);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.step-section.state-collapsed { opacity: 0.6; }
.step-section.state-done { background: #f7fbff; }
.step-section.state-active { border: 2rpx solid #1e88e5; }

.step-head {
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-height: 64rpx;
}

.step-num {
  width: var(--step-num, 22px);
  height: var(--step-num, 22px);
  border-radius: 50%;
  background: #ccc;
  color: #fff;
  font-size: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  font-weight: 600;
}

.state-active .step-num { background: #1e88e5; }
.state-done .step-num { background: #4caf50; }

.step-title {
  font-size: var(--body-font, 14px);
  font-weight: 600;
  color: var(--color-text-primary);
  flex-shrink: 0;
}

.step-summary {
  font-size: var(--meta-font, 12px);
  color: var(--color-text-secondary);
  margin-left: auto;
  text-align: right;
  /* 巨字号下两行截断，常规单行省略 */
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 1;
}
.page-container.huge .step-summary { -webkit-line-clamp: 2; }

.step-placeholder {
  font-size: var(--meta-font, 12px);
  color: var(--color-text-tertiary);
  margin-left: auto;
}

.step-body {
  margin-top: var(--label-mt, 12px);
  padding-top: var(--label-mt, 12px);
  border-top: 1rpx solid var(--color-divider);
}

.step-next-btn {
  margin-top: var(--label-mt, 12px);
  height: var(--primary-btn-h, 42px);
  background: #1e88e5;
  color: #fff;
  font-size: var(--body-font, 14px);
  border-radius: 12rpx;
}
.step-next-btn[disabled] { background: #b0bec5; }

.service-option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--label-mt, 12px) var(--spacing-md);
  border: 2rpx solid var(--color-divider);
  border-radius: 12rpx;
  margin-bottom: var(--label-mb, 6px);
  font-size: var(--body-font, 14px);
}
.service-option.active { border-color: #1e88e5; background: #e3f2fd; }
.service-name { font-weight: 500; color: var(--color-text-primary); }
.service-price { color: #1e88e5; font-weight: 600; }
.service-check { color: #1e88e5; margin-left: 8rpx; }

.picker-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--label-mt, 12px) 0;
  font-size: var(--body-font, 14px);
}
.picker-label { color: var(--color-text-secondary); }

.period-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--label-mt, 12px) 0;
  gap: 16rpx;
}
.period-options { display: flex; gap: 12rpx; }
.period-option {
  padding: 12rpx 32rpx;
  border: 2rpx solid var(--color-divider);
  border-radius: 12rpx;
  font-size: var(--body-font, 14px);
  color: var(--color-text-secondary);
}
.period-option.active {
  border-color: #1e88e5;
  background: #e3f2fd;
  color: #1e88e5;
  font-weight: 600;
}

.sub-section { margin-bottom: var(--spacing-md); }
.sub-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--label-mb, 6px);
}
.sub-label {
  font-size: var(--body-font, 14px);
  color: var(--color-text-secondary);
  font-weight: 500;
}

.select-hospital-btn {
  width: 100%;
  height: var(--primary-btn-h, 42px);
  background: #f5f5f5;
  color: var(--color-text-secondary);
  border-radius: 12rpx;
  font-size: var(--body-font, 14px);
}

.submit-btn[disabled] {
  background: #b0bec5;
  color: #fff;
}
.page-container.huge .submit-btn {
  height: var(--primary-btn-h, 50px);
  font-size: var(--body-font, 18px);
}
