# Build Scripts

Run `node wechat/scripts/inject-version.js` before uploading the mini program to inject version, git sha, and build time into the about page. Use `--dry-run` to preview without writing.
