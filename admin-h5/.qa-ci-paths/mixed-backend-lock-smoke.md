# QA CI paths validation — mixed bad backend lock smoke

Temporary validation file for S2-OPS-004 acceptance #3.
This PR touches admin-h5 and backend. The backend test is intentionally failing, so Backend Tests must fail and block merge.
